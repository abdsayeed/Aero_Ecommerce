// Shared product type used by Card, CartStore, and homepage static data
export type Product = {
  id: string;
  name: string;
  brand: string;
  price: string;
  image: string;
  category: string;
  description: string;
};
